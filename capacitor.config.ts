import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.challenge.FeaturedContent',
  appName: 'feature-content',
  webDir: 'build'
};

export default config;
